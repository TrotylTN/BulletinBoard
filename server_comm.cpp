#include "server_comm.h"

int RegisterThisServer(string coor_addr,
                       int coor_port,
                       string self_addr,
                       int self_port,
                       int socket_fd) {
  string message_send = "RR";
  return -1;
}
