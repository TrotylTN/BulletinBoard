#include "server_comm.h"

void QuorumServer(string coor_addr,
                      int coor_port,
                      string self_addr,
                      int self_port,
                      int socket_fd) {
  return;
}

void QuorumServerCoor(string self_addr,
                      int self_port,
                      int socket_fd) {
 return;                        
}
