#include "server_comm.h"

void RAndWServer(string coor_addr,
                 int coor_port,
                 string self_addr,
                 int self_port,
                 int socket_fd) {
  return;
}
void RAndWServerCoor(string self_addr,
                     int self_port,
                     int socket_fd) {
 return;                       
}
